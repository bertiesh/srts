const getters = {
    userInfo: state => state.user.userInfo,
    usermsg:state => state.user.usermsg,
    webmsg:state => state.user.webmsg,
    website: state => state.user.website,
  }
  export default getters 